pagespeed:
  build: docker/pagespeed
  environment:
    COOKIES:
    BACKEND:
    BACKENDS:
    FRONTEND:
    SERVER_NAME:
    PROXY_DOMAINS:
    PROXY_HTTPS_DOMAINS:
    ALLOW_ROBOTS:
    FILTERS_ON:
    FILTERS_OFF:
    FILTERS_ENABLED:
    FILTERS_DISABLED:
    CUSTOM_SETTINGS:
    MEMCACHED:
    MEMCACHED_SIZE:
    MAX_REQUESTS:
    HEALTCHECK:
    TTL:
    S_MAXAGE:
    GRACE:
    S_MAXAGE:
    DEBUG:
    IF_DESKTOP:
    IF_MOBILE:
  ports:
   - "80:80"
   - "8080:8080"
   
   